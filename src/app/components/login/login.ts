import { Component } from '@angular/core';
import { Page } from '../page/page';

@Component({
  selector: 'app-login',
  imports: [Page],
  templateUrl: './login.html',
  styleUrl: './login.scss',
})
export class Login {}
